name: CodeQL

on:
  workflow_dispatch:

permissions:
  contents: read
  security-events: read

jobs:
  disabled:
    name: Disabled (use GitHub Default CodeQL setup)
    runs-on: ubuntu-latest
    steps:
      - run: echo "This repository uses GitHub default CodeQL setup. Custom workflow is disabled."
